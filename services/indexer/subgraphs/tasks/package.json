{
  "name": "@covenant/tasks-subgraph",
  "version": "1.0.0",
  "description": "TheGraph subgraph for COVENANT Protocol task indexing",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ covenant-protocol/tasks",
    "create-local": "graph create --node http://localhost:8020/ covenant-protocol/tasks",
    "remove-local": "graph remove --node http://localhost:8020/ covenant-protocol/tasks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 covenant-protocol/tasks",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
